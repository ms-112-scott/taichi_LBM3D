# Gemini Code Companion: taichi_LBM3D

## 專案概述 (Project Overview)

`taichi_LBM3D` 專案是一個基於 Taichi 語言的高性能**計算流體力學（CFD）數據集生成工具**。其核心目標不是進行單次模擬，而是為一項關於**「即時互動建築物理場設計」**的碩士研究，提供大量高品質的**動態（Time-series）流場數據**。

此專案利用晶格波茲曼方法（Lattice Boltzmann Method, LBM）來模擬三維流體。LBM 的計算結構與神經細胞自動機（NCA）高度同構，使其生成的數據集特別適合用於訓練 AI 代理模型（Surrogate Model）。

透過 Taichi 的大規模並行計算能力，求解器可以在 GPU 上高效運行，極大地縮短了生成數千幀連續數據所需的時間，這是傳統 CPU 求解器（如 OpenFOAM）難以企及的。

專案生成的數據將作為**「AI 的教科書」**，用於訓練一個**模組化神經細胞自動機（m-NCA）**模型，使其學會流體運動的物理規則，最終實現一個能對設計變更做出即時反應的互動式設計工具。

## 研究脈絡中的定位 (Role in the Research Context)

本專案是整個研究的**基石**，其產出（LBM 動態數據集）是後續所有 AI 模型訓練與驗證的基礎。

整個研究的「黃金主線」旨在解決傳統 CFD 工具模擬速度慢、與設計流程脫節的「認知斷裂」問題。其技術演進路徑如下：

1.  **速度與輕量化 (CAADRIA 2025):** 驗證 NCA 作為 CFD 代理模型的可行性，實現千倍提速。
2.  **準確度與可信賴性 (CAADRIA 2026):** 發展 **m-NCA** 架構，引入物理約束（PINN Loss），解決泛化性與黑箱問題。
3.  **連續性與互動性 (eCAADe):** 實現**狀態連續模擬**，讓 AI 模型能即時適應設計的動態修改，無需重置。

在此脈絡下，本專案扮演的角色是：

-   **主要訓練數據來源：** 使用 **Taichi LBM** 生成的**高動態、網格原生**的數據來訓練 m-NCA。這能確保 AI 學到的是流場的「動態過程」，而非靜態結果。
-   **對照組的基準：** 生成的數據也可用於訓練其他 AI 模型（如 U-Net），以凸顯 NCA 在輕量化與效率上的優勢。
-   **與黃金標準的區別：** 研究中的「黃金驗證標準」將由 OpenFOAM 提供，但僅用於最終驗證，**不參與訓練**。本專案的 LBM 數據才是驅動整個模型學習的核心。

## 數據生成流程 (Data Generation Workflow)

所有求解器皆透過修改其 Python 文件頂部的參數來配置。生成數據集的工作流程如下：

1.  **選擇求解器:** 根據所需物理場景選擇合適的求解器（如 `Single_phase/lbm_solver_3d.py`）。
2.  **設定計算後端:** 在 Python 腳本中，**強烈建議**將後端設為 GPU 以實現最高效率。
    ```python
    # ti.init(arch=ti.cpu)
    ti.init(arch=ti.gpu)
    ```
3.  **定義幾何邊界:**
    -   根據研究規劃，需修改求解器代碼，使其能從**外部文件（如圖片或 .dat 檔）讀取幾何形狀**，而非依賴滑鼠互動。
    -   這是當前開發的**首要任務**。
4.  **配置模擬與輸出參數:**
    -   設定網格尺寸 (`nx`, `ny`, `nz`)、流體屬性 (`niu`)、驅動力 (`fx`) 等。
    -   設定模擬總步長（例如 2000 幀）以及數據匯出的頻率。
    -   確保輸出格式為可以直接被 PyTorch/TensorFlow 讀取的張量（Tensor）格式（如 `.npy` 或 `.npz`）。
5.  **批量運行:**
    -   撰寫一個外部 Python 腳本，該腳本可以遍歷一個包含數十種不同幾何定義文件的資料夾。
    -   對於每種幾何，調用 LBM 求解器執行模擬，並將輸出的時序數據儲存到指定的資料夾中，最終形成 `Taichi-Dynamic-Dataset_v1`。

## 專案結構 (Project Structure)

-   `Single_phase/`: 包含單相流 LBM 求解器，是目前研究的核心。
-   `2phase/`: 包含兩相流 LBM 求解器。
-   `Grey_Scale/`: 包含灰度 LBM 求解器。
-   `my_docs/`: 包含研究的核心脈絡、規劃與相關說明文件。
-   `requirements.txt`: Python 依賴項。

## 安裝 (Installation)

1.  **Taichi:** 核心依賴項是 Taichi 程式語言。
    ```bash
    python3 -m pip install taichi
    ```
2.  **PyEVTK (可選):** 用於將結果匯出為 VTK 格式進行傳統視覺化。對於數據生成本身非必需。
    ```bash
    pip install pyevtk
    ```
3.  **Numpy:** 用於數據處理與文件保存。
    ```bash
    pip install numpy
    ```

## 開發慣例 (Development Conventions)

-   **配置:** 所有模擬參數都硬編碼在求解器文件的頂部。
-   **當前任務:** 根據 `my_docs/進度規劃.md`，目前 (W1: 01/27-02/02) 的開發重點是修改 LBM 求解器，使其**支持從文件讀取邊界條件**，並準備進行批量化的數據生成。專注於後端腳本，暫不開發 UI。

## 檔案歸類

-   gemini 的說明md文件都放置於`my_docs/gemini/`底下 可以自行在創建子資料夾用於歸類，解釋性的doc不要用`GEMINI.md`檔案名稱 使用其他命名，並且明文件除非特別說明不然都創建新的檔案。

## 語言習慣

-   解釋性文字(程式註解 或是 說明文件) 都使用繁體中文。
-   code使用python 並且符合pythonic原則 使用清楚簡潔註解與標示，加上type hint。